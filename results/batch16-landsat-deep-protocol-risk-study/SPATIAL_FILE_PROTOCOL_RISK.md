# Spatial/File Protocol Risk

Random split changed macro-F1 by 0.0222. The source files remain the stronger evaluation target because the documentation warns against cross-validation and the dataset is image-derived.

Risk mechanism classification: `spatial_file_protocol_risk`.
