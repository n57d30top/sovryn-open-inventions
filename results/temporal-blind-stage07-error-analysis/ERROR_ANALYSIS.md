# Error Analysis

The larger blind run found 47 wrong or partial predictions. The instrument remains useful as a conservative filter, but label separation is not clean enough for the strongest status.
