# Acceleration Measurement

Time-to-evidence is measured per routed target and per route. The benchmark compares the route harness with a conservative manual triage estimate. This only supports bounded task-level timing observations, not a broad acceleration claim.
