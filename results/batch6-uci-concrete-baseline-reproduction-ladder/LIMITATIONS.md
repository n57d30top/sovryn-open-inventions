# Limitations

- The comparison is one deterministic split, not a full benchmark sweep.
- The random forest is a conventional challenger, not a new method.
- The duplicate-row finding should be considered during future benchmark design.

## Safety scope

Safe civil-engineering tabular regression dataset with public non-sensitive data.
