# Replication

This result includes reproducibility instructions and deterministic aggregate outputs. It is not a full independent multi-environment replication unless explicitly stated. Future work should repeat the run with fresh splits, fresh containers, and dependency variants where relevant.
