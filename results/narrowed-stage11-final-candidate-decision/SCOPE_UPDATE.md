# Scope Update

The discovery-candidate scope is rejected. A narrower audit-method scope remains: receipt stability plus baseline pressure can help decide when not to trust descriptor residual claims.
