# Package Manifest Requirements

Every public package needs package id, target id, route, receipt id, result summary, limitations, reproduce steps where applicable, and hygiene flags for raw logs and local paths.
