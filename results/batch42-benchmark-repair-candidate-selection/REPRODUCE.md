# Reproduce

Use a Python environment with pandas, numpy, scikit-learn, requests, and unlzw3. Load the public sources named in the target cards, freeze prediction cards first, then run source/random/repaired comparisons with seed 42. Public artifacts summarize commands and metrics without command journals.
