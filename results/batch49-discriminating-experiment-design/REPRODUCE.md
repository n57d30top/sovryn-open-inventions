# Reproduce

Use the public corpus artifacts referenced in each card. For executable tests, use Python with pandas, numpy, scikit-learn, requests, and unlzw3. Keep prediction cards frozen before running any execution, then compare observations against the preregistered predictions.
