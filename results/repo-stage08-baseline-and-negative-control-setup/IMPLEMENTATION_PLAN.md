# Implementation Plan

For each frozen target, retrieve public metadata, run isolated install, run smoke/runtime check, optionally run pytest probe, compare to static checklist and docs-only baselines, then record a curated receipt summary.
