# Temporal v2 Design

Temporal v2 narrows the class to targets where temporal/random split, horizon/window stress, shuffled-time control, leakage control, baseline comparison, and replay sampling can be recorded.
