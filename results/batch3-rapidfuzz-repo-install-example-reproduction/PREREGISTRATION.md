# Preregistration

Research question: Can RapidFuzz be installed and operated as a small evidence-matching tool without claiming full upstream reproduction?

Metrics fixed before execution:
- install success
- installed version
- example checks passed
- example checks failed
- full upstream tests executed or not

Kill criteria:
- package install fails
- example checks fail
- upstream test-suite success claimed without execution
