# Method

Install the target package as a problem-specific fuzzy evidence-matching instrument and run bounded example checks.

Attempted ideas:
- RapidFuzz as fuzzy source/evidence matching tool
- full repo reproduction rejected because upstream tests were not run
