# Reproduce

1. Create a Python virtual environment.
2. Install rapidfuzz from PyPI.
3. Run fuzz.ratio, fuzz.partial_ratio, and process.extractOne checks.
4. Do not claim upstream test reproduction unless tests are executed.

Keep outputs public-safe and avoid publishing raw logs or private data.
