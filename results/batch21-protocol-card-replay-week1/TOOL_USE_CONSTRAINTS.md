# Tool Use Constraints

`metric_stress_validator` remains a support tool for anti-hype controls. `schema_provenance_auditor` remains packaging-only. Protocol Cards are not product framework features; they are public result artifacts.
