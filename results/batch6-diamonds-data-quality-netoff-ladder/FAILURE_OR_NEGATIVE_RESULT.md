# Failure Or Negative Result

Result label: `data_quality_issues_confirmed_under_network_off_replay`

The negative outcome is that the public CSV contains duplicate rows and nonpositive dimension values. This is useful data-quality evidence, not a claim that the dataset is unusable.
