# Install Or Provisioning

Provisioned items:

- pandas 3.0.2

Environment creation succeeded for the bounded execution path. Docker container-netoff provisioning was attempted but unavailable, so the replay used a container-netoff-equivalent sandbox-exec profile with network access denied.

No host package manager, privileged install, private data source, or new standalone repository was used.
