# Next Frontier Program

Selected: candidate rejection and new discovery search.

Reason: a larger validation on this candidate would likely spend effort on baseline-dominated residuals. A new search should use the failure map as constraints.
