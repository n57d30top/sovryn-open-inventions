# Method

Parse the public data file and metadata, then compare shape-only checks with type-aware missing-token checks.

Attempted improvements or challengers:
- type-aware missing token audit
- schema-only audit baseline
