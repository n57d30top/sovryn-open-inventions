# Repo Deep Reproduction Design

Repo reproduction is split into static scan, install probe, smoke runtime, example runtime, partial test runtime, full test runtime, fresh workspace replay, and container replay tiers.
