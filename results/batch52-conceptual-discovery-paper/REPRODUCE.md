# Reproduce

Use the frozen cards and public sources listed in the artifacts. Execute only after freezing. Use Python with pandas, numpy, scikit-learn, requests, and unlzw3. Package checks use safe import/version/runtime assertions.
