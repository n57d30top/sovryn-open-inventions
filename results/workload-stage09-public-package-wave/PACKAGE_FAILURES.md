# Package Failures

Package failures and non-package cases were retained as evidence rather than hidden. Main causes: insufficient evidence completeness, low package readiness, unsafe rejection, not-testable confirmation, fallback route use, and ambiguous target metadata.
