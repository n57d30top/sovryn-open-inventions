# Getting Started OS v1

1. Pick a safe public target.
2. Classify it with `sovryn route classify --target <target> --json`.
3. Plan the route with `sovryn route plan --target <target> --json`.
4. Execute the minimum-evidence route.
5. Read limitations before interpreting the package.
