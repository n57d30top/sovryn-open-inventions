# Method / Audit

A schema-aware public protocol parser recovered all four required evaluation fields from the external README/evaluation instructions; this supports a narrow protocol-extraction extension, not full benchmark superiority.

This method/audit is target-specific and not a new platform layer.
