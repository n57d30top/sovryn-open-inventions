# Reproduce

Use the curated target package names and public PyPI URLs in this result. Retrieval uses HTTPS package metadata. Install attempts use isolated Python target directories, followed by import or smoke/example commands. Public artifacts intentionally omit raw logs and local paths.

Result slug: repo-stage05-static-vs-runtime-evidence-wave2
