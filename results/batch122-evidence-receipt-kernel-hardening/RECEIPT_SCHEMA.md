# Receipt Schema

Receipts include target id, claim id, public source URL, retrieval method, timestamp, artifact hash, public-safe environment summary, package versions, command summary, install/execution/baseline/control/replay flags, failure reason, redaction status, and scope. Fixtures or mock outputs do not count toward external quotas.
