# Tooling Plan

Use existing Python scientific packages, package download/install checks, local isolated execution, simple baselines, shuffled controls, and public corpus audit gates. No new framework layer or standalone repository is introduced.
