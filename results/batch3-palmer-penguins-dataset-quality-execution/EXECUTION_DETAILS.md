# Execution Details

Data was fetched once from the public CSV URL, then processed by a Node script inside a Docker container with network disabled, CPU and memory limits, and no host sudo. The public package includes only aggregate metrics.

Execution evidence is summarized and redacted. Raw install logs, raw stdout/stderr, local paths, secrets, and private configuration are excluded.
