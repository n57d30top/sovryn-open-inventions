# Reproduce

1. Fetch the Palmer Penguins CSV from seaborn-data.
2. Run a CSV parser that counts rows, columns, missing values, and duplicate rows.
3. Compare schema-only checks with field-aware missingness checks.

Keep outputs public-safe and avoid publishing raw logs or private data.
