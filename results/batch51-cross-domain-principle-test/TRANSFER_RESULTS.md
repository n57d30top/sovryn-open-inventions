# Transfer Results

The principle transferred best to benchmark/protocol, dataset reliability, and time-series cases. It transferred only partially to package/repo runtime checks because those domains support narrower runtime claims, not benchmark-validity claims.
