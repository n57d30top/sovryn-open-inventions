# Next Frontier Program

Selected: public_search_index_layer.

Rationale: the OS v1 Candidate package is now public and inspectable, but users need a better static search/index layer to navigate packages, domain packs, classes, routes, failure modes, and limitations.
