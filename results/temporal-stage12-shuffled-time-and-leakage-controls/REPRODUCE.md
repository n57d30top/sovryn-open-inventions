# Reproduce

Use the public source URLs and frozen prediction cards in this result. Re-run retrieval, temporal split, random challenger, baseline, control, holdout, and replay checks. Frozen prediction cards must not be edited after the freeze commit.
