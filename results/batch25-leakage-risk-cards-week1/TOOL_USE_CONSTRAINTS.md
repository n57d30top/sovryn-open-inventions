# Tool Use Constraints

Leakage-Risk Cards are public result artifacts, not a new framework layer. Hash overlap, group overlap, feature-target screening, metric stress, and schema/provenance checks are support tools. No result may claim leakage unless direct overlap or target-like evidence supports it.
