# Install Or Provisioning

Batch 25 used an isolated Python environment and public UCI sources.

Package versions:

```json
{
  "python": "3.14.4",
  "numpy": "2.4.4",
  "pandas": "3.0.2",
  "scikit_learn": "1.8.0"
}
```

No private data, unsafe target domains, host sudo, or new product service was used.
