# False Rejects

False quick-reject risk remains possible for ambiguous public targets, especially where metadata is weak. v2 reduced unsupported quick rejection by requiring confirmation, but kill week still flagged conservative reject risk.
