# Falsification

The result is bounded by route errors, package-quality caveats, and kill-week revisions. Claims are downgraded when evidence is incomplete, package quality is weak, route choice is uncertain, or acceleration estimates depend on a fragile baseline.
