# Ambiguity Reconstruction

The archive contains multiple partition-like files (`xaa` through `xai`) plus documentation. The files can be interpreted as folds or file partitions, but this run did not find enough source evidence to treat one interpretation as the official benchmark protocol. Ambiguity class: file_layout_ambiguous and protocol_ambiguous.
