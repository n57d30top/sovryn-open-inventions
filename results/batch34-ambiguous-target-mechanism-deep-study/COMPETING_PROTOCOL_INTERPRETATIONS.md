# Competing Protocol Interpretations

1. Hold out `xai` as a file-based test partition.
2. Hold out `xah` and `xai` as a stricter file-based test partition.
3. Ignore file partitions and use a stratified random 25 percent challenger split.

All three are plausible enough for stress testing, but none is asserted as official.
