# Protocol Card Decision

Decision: `ambiguous_needs_human_protocol_clarification`.

Vehicle is executable and useful as an ambiguity stress case, but it should not be used as a strong protocol-first validation target until its file/fold semantics are clarified.
