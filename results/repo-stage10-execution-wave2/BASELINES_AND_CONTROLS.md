# Baselines and Controls

Static checklist, docs-only, tests-present, dependency-pinned, minimal smoke, random/control package, and pytest-probe baselines were compared against observed runtime behavior.
