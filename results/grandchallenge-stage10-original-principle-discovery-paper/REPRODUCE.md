# Reproduce

Use the public URLs in the prediction cards. Fetch each public dataset or package, compute hashes, run the described simple baseline, negative control, and replay checks. For npm packages, install with lifecycle scripts disabled and run the listed smoke checks. Publish only receipt summaries, not raw logs.
