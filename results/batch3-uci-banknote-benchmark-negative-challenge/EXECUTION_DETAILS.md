# Execution Details

Public data was fetched, then a deterministic split and metric computation ran in a network-off Docker container. The challenger was frozen before holdout scoring and rejected by the preregistered F1 kill criterion.

Execution evidence is summarized and redacted. Raw install logs, raw stdout/stderr, local paths, secrets, and private configuration are excluded.
