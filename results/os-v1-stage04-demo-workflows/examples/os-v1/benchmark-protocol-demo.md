# Benchmark Protocol Audit Demo

- Input: A benchmark protocol, scoring description, and public result table.
- Command: `sovryn route classify --target benchmark-protocol-demo --json && sovryn route execute --target benchmark-protocol-demo --json`
- Expected package: benchmark protocol brief with route, minimum evidence, limitations, and package status
- Interpretation: Use the output to separate protocol clarity from result validity.
- Limitations: Does not prove benchmark superiority.
