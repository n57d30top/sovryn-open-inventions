# Frozen Route Predictions

160 route predictions were frozen before execution. Each card records predicted target type, predicted route, route confidence, expected cost, expected package status, expected failure mode, and fallback route.
