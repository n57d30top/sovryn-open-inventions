# Reproduce

Run the public validation checks with the repository root as the working directory. Prediction freeze artifacts are published before holdout selection. Public files contain receipt summaries only; detailed local execution traces are excluded from the public package.

Stage: stage05.
