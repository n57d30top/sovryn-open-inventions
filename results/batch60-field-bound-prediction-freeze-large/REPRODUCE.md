# Reproduce

Use the curated public artifacts in this result directory. Execution summaries were produced from public packages, public example datasets, public repo metadata, and prior public corpus artifacts. The package intentionally excludes command journals and machine-specific paths.
