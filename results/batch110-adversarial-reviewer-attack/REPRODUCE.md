# Reproduce

Use an isolated Python environment with the packages and public sources listed in the artifacts. Re-run the described package downloads/imports, dataset loads, baseline checks, negative controls, temporal checks, and replay attempts. Keep raw transcripts private and publish only curated summaries.
