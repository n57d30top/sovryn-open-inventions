# Reproduce

Provision Python with pandas, numpy, scikit-learn, requests, and unlzw3. Load the public UCI archives, verify Batch 32 preregistration hashes, then run source/protocol and random/stratified comparisons with fixed seeds. Public outputs summarize commands without command streams.
