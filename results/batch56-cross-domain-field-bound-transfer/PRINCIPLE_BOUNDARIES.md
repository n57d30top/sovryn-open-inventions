# Principle Boundaries

- The principle is strongest when required fields are observable.
- It is weaker when the domain lacks explicit field identifiers.
- It must produce not-testable labels rather than invent missing fields.
