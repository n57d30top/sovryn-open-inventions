# Reproduce

- Run `sovryn route v3-audit --json` for the Product audit.
- Run `sovryn route class-score --json` for class-level scoring.
- Run `sovryn route compare-policy --from v2 --to v3 --json` for policy comparison.
- Inspect public package artifacts in this result directory.
