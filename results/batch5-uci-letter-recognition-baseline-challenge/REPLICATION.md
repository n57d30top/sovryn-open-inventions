# Replication

This result includes deterministic reproduction instructions and aggregate outputs. It is not a full independent multi-environment replication unless explicitly stated.

Future work should repeat the run with fresh containers, alternate dependency versions, or alternate deterministic splits where relevant.
