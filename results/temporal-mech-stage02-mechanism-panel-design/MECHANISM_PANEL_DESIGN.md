| label | falsifier | downgrade rule |
| --- | --- | --- |
| true_temporal_fragility_candidate | true_temporal_fragility_candidate-baseline, true_temporal_fragility_candidate-artifact, true_temporal_fragility_candidate-replay | downgrade true_temporal_fragility_candidate when a rival panel explains the same signal with fewer unresolved caveats |
| baseline_dominated | baseline_dominated-baseline, baseline_dominated-artifact, baseline_dominated-replay | downgrade baseline_dominated when a rival panel explains the same signal with fewer unresolved caveats |
| random_split_artifact | random_split_artifact-baseline, random_split_artifact-artifact, random_split_artifact-replay | downgrade random_split_artifact when a rival panel explains the same signal with fewer unresolved caveats |
| horizon_sensitive | horizon_sensitive-baseline, horizon_sensitive-artifact, horizon_sensitive-replay | downgrade horizon_sensitive when a rival panel explains the same signal with fewer unresolved caveats |
| window_sensitive | window_sensitive-baseline, window_sensitive-artifact, window_sensitive-replay | downgrade window_sensitive when a rival panel explains the same signal with fewer unresolved caveats |
| leakage_artifact | leakage_artifact-baseline, leakage_artifact-artifact, leakage_artifact-replay | downgrade leakage_artifact when a rival panel explains the same signal with fewer unresolved caveats |
| shuffled_time_artifact | shuffled_time_artifact-baseline, shuffled_time_artifact-artifact, shuffled_time_artifact-replay | downgrade shuffled_time_artifact when a rival panel explains the same signal with fewer unresolved caveats |
| replay_unstable | replay_unstable-baseline, replay_unstable-artifact, replay_unstable-replay | downgrade replay_unstable when a rival panel explains the same signal with fewer unresolved caveats |
| low_risk_control | low_risk_control-baseline, low_risk_control-artifact, low_risk_control-replay | downgrade low_risk_control when a rival panel explains the same signal with fewer unresolved caveats |
| inconclusive | inconclusive-baseline, inconclusive-artifact, inconclusive-replay | downgrade inconclusive when a rival panel explains the same signal with fewer unresolved caveats |
