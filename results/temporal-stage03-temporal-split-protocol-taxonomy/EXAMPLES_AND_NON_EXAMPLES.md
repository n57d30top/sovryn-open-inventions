# Examples and Non-Examples

Example: high-autocorrelation monthly series where random split looks much easier than chronological holdout. Non-example: unordered static table with a year column that does not define a valid temporal task.
