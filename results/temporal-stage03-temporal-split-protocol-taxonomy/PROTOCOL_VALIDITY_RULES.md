# Protocol Validity Rules

- chronological split: valid only when time/index fields and target fields are explicit enough for replay.
- rolling window split: valid only when time/index fields and target fields are explicit enough for replay.
- blocked time split: valid only when time/index fields and target fields are explicit enough for replay.
- expanding window: valid only when time/index fields and target fields are explicit enough for replay.
- random split challenger: valid only when time/index fields and target fields are explicit enough for replay.
- shuffled-time control: valid only when time/index fields and target fields are explicit enough for replay.
- horizon holdout: valid only when time/index fields and target fields are explicit enough for replay.
- season-aware split: valid only when time/index fields and target fields are explicit enough for replay.
- leakage-prone split: valid only when time/index fields and target fields are explicit enough for replay.
- low-risk non-temporal control: valid only when time/index fields and target fields are explicit enough for replay.
