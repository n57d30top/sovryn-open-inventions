# Fund Gate Definition

A candidate can notify the user only if every Fund Gate passes. The evaluator checks candidate identity, high-impact safe domain, nontriviality, rival pressure, baseline resistance, counterexample pressure, frozen predictions, holdout support, replay/reproduction, proof or mechanism pressure, kill week status, and an external-review package.

If any major gate fails, the daemon returns `continue_searching` and suppresses notification.
