# Build vs Buy

Existing tools were tried first through package-level operations, simple shape/count checks, or baseline command behavior. The gap was not basic execution; the gap was producing a small, repeatable, public-safe evidence card for the specific research question.

Decision: build `schema_provenance_auditor` and keep the implementation minimal.

Tool decision after execution: `keep_as_reusable_candidate`.
