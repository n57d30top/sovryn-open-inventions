# Baseline Comparison

A manual pandas shape and duplicate check can inspect each file separately, but it does not bind both split files to source hashes and a single schema comparison record.

The custom tool was compared against the simpler baseline/manual check above.
