# Execution Summary

Downloaded the two public CSV files, then ran schema_provenance_auditor inside a network-denied sandbox profile after the files were local.

The tool audited 6497 rows across two files, found schema_match=true, missing_cells=0, and duplicate_full_rows=1177.

Only curated measurements are published here.
