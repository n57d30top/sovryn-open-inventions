# Next Research Direction

Batch 12 should review Batches 5-11 and select the next focused program based on evidence.

For the dataset-and-metric direction, the rule should be stricter:

- Keep `metric_stress_validator` as a reusable support tool.
- Treat `schema_provenance_auditor` as packaging-first unless it finds a pandas-missed issue.
- Prefer targets with official splits, source protocols, group metadata, or known evaluation hazards.
- Do not count high baseline accuracy as a research advance.
- Require every future tool claim to state what it adds beyond a named simple baseline.
