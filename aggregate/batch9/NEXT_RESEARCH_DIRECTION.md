# Next Research Direction

Continue dataset and metric validation, but require a target with an official or paper-defined split when possible.

The next run should test whether duplicate-aware and class-risk reporting generalizes beyond Dry Bean and whether the schema tool can remain reusable when the target includes source-provided split metadata.
