# Next Batch 9 Deep Target Brief

Use the next batch for one deep external target in dataset and metric validation.

Preferred target shape:

- Public safe dataset or benchmark.
- Clear baseline task.
- Enough metadata to define a defensible split or repeated evaluation.
- Suitable for `metric_stress_validator`.
- Suitable for a narrowed `schema_provenance_auditor` check if the data has related files or splits.

Do not add framework features. Use the existing tools and corpus pipeline.
